language: go
go:
  - 1.6

sudo: required

services:
  - docker

branches:
  only:
    - master
    - /^test_/
    - /^test-/

cache:
  edge: true
  directories:
    - $HOME/google-cloud-sdk

env:
  - GOARCH=amd64 GOOS=linux

script:
  - go test -v ./... && ./travis_docker_push.sh
